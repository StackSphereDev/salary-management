{
  "version": 2,
  "env": {
    "DATABASE_URL": "file:/tmp/dev.db"
  },
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
