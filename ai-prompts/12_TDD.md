Write tests for employee seed generation script.

Requirements:
- generates 10k employees
- generates unique emails
- combines first and last names
- produces valid salary ranges
- deterministic random generation

Do not implement script yet.